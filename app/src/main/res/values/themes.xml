<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.InventarioLPY" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/red</item> <!-- Color de la barra de acción -->
        <item name="colorPrimaryDark">@color/red</item> <!-- Color de la barra de estado -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/red</item> <!-- Color de la barra de estado -->
    </style>
    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@color/zxing_transparent</item> <!-- Fondo blanco -->
        <item name="android:windowIsFloating">true</item>
    </style>
</resources>